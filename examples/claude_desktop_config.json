{
  "_comment": "Drop the appropriate block into ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) or %APPDATA%/Claude/claude_desktop_config.json (Windows). Restart Claude Desktop after.",

  "mcpServers": {
    "sqlite-readonly": {
      "command": "python",
      "args": [
        "-m", "mcp_sqlite_server",
        "/absolute/path/to/your/database.db"
      ]
    },
    "sqlite-readwrite": {
      "command": "python",
      "args": [
        "-m", "mcp_sqlite_server",
        "/absolute/path/to/another/database.db",
        "--allow-writes"
      ]
    }
  }
}
